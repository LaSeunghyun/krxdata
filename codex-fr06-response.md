| 항목 | 권고 결론 | 이유 |
|---|---|---|
| Q1/Q2 판정 기준 | **하이브리드 권고: 구조 변경 잠금은 `캠페인/광고 생성 여부`, 운영 표시만 `게재 상태` 사용** | 게재 상태는 OFF/일시정지/예산/기간/심사 등으로 변하는 런타임 값이라 “패키지 구성 변경 가능 여부”의 기준으로 부적합 |
| Q2 루프홀 | **기 생성 캠페인이 1개라도 있으면 게재위치 직접 추가 불가**. 추가가 필요하면 별도 “패키지 확장/보정 플로우”로만 허용 | OFF → 지면 추가 → ON 시 기존 광고 세트가 새 지면을 커버하지 못해 all-or-nothing 전제가 깨짐 |
| Q3 개별 on/off | **패키지 소속 광고는 개별 토글 비활성화 또는 묶음 토글만 허용** | 일부 광고만 OFF가 되면 UI상 패키지 전체 노출 가능처럼 보이지만 실제로는 전체 미노출 조건이 됨 |
| 사용자 1차 안 | **FR-08 보정 플로우로는 유효, FR-06 구조 잠금 해제 근거로는 부족** | “누락 그룹만 광고 추가”는 좋지만, 지면 추가 자체를 열어주는 정책이면 동일한 루프홀이 남음 |

**추천 정책**
FR-06의 핵심은 “운영 중 여부”가 아니라 **패키지 구조 잠금 여부**로 재정의하는 것이 맞습니다.

권고 문구는 이렇게 잡는 편이 안전합니다.

| 상태 | 소재 스펙 추가 | 그룹 추가 | 게재위치 추가 | 광고 추가 |
|---|---:|---:|---:|---:|
| 패키지 상품만 있고 캠페인/광고 없음 | 가능 | 가능 | 가능 | 해당 없음 |
| 해당 상품으로 캠페인 또는 광고그룹 생성됨 | 불가 | 불가 | 불가 | 조건부 가능 |
| 일부 게재위치 그룹에 광고 누락 | 불가 | 불가 | 불가 | **누락 그룹 보정 목적만 가능** |
| 모든 그룹 광고 구성 완료 | 불가 | 불가 | 불가 | 일반 추가 불가 또는 동일 구조 유지 조건부 |

즉, `게재 상태 = 정상/일부 노출`은 “현재 운영 중처럼 보이는가”를 표시하는 보조값으로만 쓰고, 버튼 활성화의 기준은 **해당 패키지를 참조하는 캠페인/광고그룹/광고 생성 여부**로 잡아야 합니다.

**Q2 루프홀 닫는 방법**
가장 단순한 해법은 다음입니다.

1. 해당 패키지로 생성된 캠페인이 있으면 게재위치 추가 버튼은 항상 비활성화한다.
2. 예외적으로 패키지 확장이 꼭 필요하면 기존 상품을 직접 수정하지 않고 다음 중 하나로 처리한다.
   - 새 패키지 버전 생성
   - 새 상품 생성 후 신규 캠페인부터 적용
   - 운영자 전용 보정 플로우에서 모든 기존 캠페인/광고그룹에 누락 광고를 동시에 생성

과도한 엔지니어링을 피하려면 **1번 + FR-08 보정 플로우**가 현실적입니다. 버전 관리는 더 안전하지만 정책/DB/리포팅 영향이 커집니다.

**Q3 on/off UX**
패키지 소속 광고 리스트에서는 개별 광고 토글을 그대로 두면 안 됩니다.

추천 UX는 다음 중 하나입니다.

| 대안 | 설명 | 평가 |
|---|---|---|
| A. 개별 토글 비활성화 + 패키지 단위 ON/OFF | 하위 광고는 읽기 전용, 상위에서 전체 ON/OFF | **추천. 가장 명확하고 구현 부담 낮음** |
| B. 개별 토글 클릭 시 전체 묶음 토글 확인 모달 | “패키지 광고는 함께 OFF됩니다” 확인 후 전체 변경 | 가능하지만 사용자가 개별 제어 가능하다고 오해할 수 있음 |
| C. 개별 OFF 허용, 서버가 전체 미노출 | 기술적으로 가능하지만 어드민 UX가 불투명 | 비추천 |

따라서 광고 리스트에는 “패키지 상품 광고는 개별 상태를 변경할 수 없습니다” 수준의 짧은 툴팁/비활성 사유를 두고, 상태 변경은 캠페인 또는 패키지 광고 묶음 단위로만 제공하는 것이 맞습니다.

**사용자 1차 안 평가**
강점은 분명합니다. 이미 캠페인/광고그룹은 있는데 특정 게재위치 그룹 광고만 누락된 상태를 보정할 수 있고, 전체 disable보다 운영 복구성이 좋습니다.

다만 약점도 큽니다.

| 관점 | 평가 |
|---|---|
| FR-08 광고 추가 보정 | 적절함 |
| FR-06 게재위치 추가 허용 근거 | 부적절함 |
| all-or-nothing 보장 | “누락 그룹 광고 추가”까지만 보장, 지면 추가/개별 OFF 문제는 해결 못 함 |
| 사용자 오해 | “OFF면 패키지 구조를 바꿔도 된다”는 잘못된 모델을 만들 수 있음 |

정리하면, 사용자 1차 안은 **“새 지면을 추가하는 정책”이 아니라 “기존 패키지 구조 내 누락 광고를 채우는 정책”**으로 제한하면 좋습니다.

**최종 추천안**
대안 1을 추천합니다.

| 대안 | 내용 | 추천도 |
|---|---|---:|
| 1. 생성 여부 기반 구조 잠금 + FR-08 누락 광고 보정 + 묶음 토글 | 가장 단순하고 정책 일관성 높음 | 높음 |
| 2. 패키지 버전 관리 | 기존 캠페인은 v1, 신규 확장은 v2로 분리 | 중간. 안전하지만 범위 큼 |
| 3. 게재 상태 기반 유지 + ON 전환 시 검증 | 늦게 막는 방식 | 낮음 |

따라서 FR-06은 이렇게 바꾸는 것이 좋습니다.

> 해당 패키지 상품으로 생성된 캠페인, 광고그룹 또는 광고가 존재하는 경우, 패키지의 소재 스펙/그룹/게재위치 구성은 변경할 수 없다. 단, 기존 패키지 구성 내에서 광고가 생성되지 않은 게재위치 그룹이 있는 경우, 광고 추가 모달에서 해당 그룹에 대한 광고 보정 추가만 허용한다. 패키지 소속 광고의 상태 변경은 개별 광고 단위가 아닌 패키지 묶음 단위로만 수행한다.


