# 정책 검토 요청 — NestAds 로드블록(패키지) 광고 FR-06 운영 중 판정

너는 B2B 광고 어드민 제품의 시니어 기획/PM이다. 아래 정책의 허점을 지적받았다. 독립적으로 판단해서 권고안을 줘. 결론 먼저, 표로.

## 배경
- "패키지(로드블록) 상품": 여러 게재위치(지면) 그룹을 하나의 상품으로 묶어 한 광고주에게 판매.
- 핵심 불변식: **전부 또는 전무(all-or-nothing)** — 패키지에 묶인 모든 지면이 함께 노출되거나, 하나라도 조건 미충족이면 전체 미노출(FR-11, 전송서버가 검증).
- 게재위치 "제거"는 운영 상태 무관 항상 불가(기존 정책).

## 현행 FR-06 정책 (검토 대상)
- 해당 상품으로 생성된 캠페인의 **게재 상태**가 "정상" 또는 "일부 노출"이면 → "운영 중"으로 판정.
- 운영 중이면 [소재 스펙 추가 / 그룹 추가 / 게재위치 추가] 버튼이 비활성화(= 운영 중 게재위치 추가 불가). 운영 중 아니면 활성화.

## 검토자가 제기한 3가지 질문
1. 판정 기준이 캠페인 "게재 상태"가 아니라 캠페인 "생성 여부"여야 하는 것 아닌가? (게재상태는 가변값이라 불안정)
2. 기 생성된 캠페인이 OFF 상태여서 운영중=false → 게재위치 추가 → 다시 ON 전환하면? (새로 추가된 지면엔 기존 광고의 소재가 없음 → 전부/전무 정합성 깨짐)
3. 패키지는 묶인 N개 광고가 모두 활성이어야 노출 조건인데, 광고 리스트에서 일부 광고만 on/off 하는 것에 대한 화면 제약이 기획에 없다. 개별 토글로 전부/전무가 깨진다.

## 사용자(기획자)의 1차 안 (광고 추가 모달 FR-08 관련)
1. 광고 추가 모달에서 캠페인/광고그룹 선택 시, 아직 광고가 추가되지 않은 게재위치 그룹이 있으면 그 그룹만 선택해 추가 가능하게 한다.
2. 추가되지 않은 게재위치 그룹이 없으면 전체 disable 유지.

## 요청
- Q1/Q2 판정 기준: 게재상태 vs 생성여부 vs 하이브리드 — 권고와 이유.
- Q2 루프홀(OFF→추가→ON)을 어떻게 닫을지.
- Q3 개별 광고 on/off 제약 UX 권고.
- 사용자 1차 안의 강점/약점 평가.
- 가능하면 2~3개 대안과 추천. 과도한 엔지니어링 금지.
