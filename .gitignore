node_modules/
.env
.env.local
claude_desktop_config.json
.corp_code_cache.json
dart-data.db
dart-data.db-shm
dart-data.db-wal
scored-*.json
profitable-stocks.json
screen-halfyear.json
.halfyear-watch-done-*
*.log
.DS_Store
.vscode/
.idea/
.update-status.json
.sector_cache.json
.news-search-cache.json
kospi-all.json
kosdaq-all.json
kospi-codes.json
kosdaq-codes.json
kospi-profitable.json
kosdaq-profitable.json
_kospi_codes.json
_kosdaq_codes.json
watchlist.json
*.db
*.db-shm
*.db-wal

# 하드코딩 시크릿 포함 — 커밋 금지 (키 로테이션 후 .env 전환 필요)
audit.mjs
audit2.mjs
backtest-apr01.mjs
backtest-longterm.mjs
backtest.mjs

# 백테스트·수집 캐시
backtest-cache-*.json
backtest_results.json
.corp_full_cache.json
.claude/settings.local.json
candles-daily.jsonl
forecast-bear-days.json
.kis-token.json
flow-snapshot-log.txt

# ── 런타임 상태·로그 (봇이 매 사이클 덮어씀 — 영구 dirty 방지, 2026-07-28) ──
stock-live-log.txt
stock-live-state.json
stock-live-journal.json
ai-shadow-log.txt
*.log.txt

# 하네스 상태·로그 (도구가 자동 생성)
.omx/
.codex/

# 백테·리서치 출력물 (재생성 가능)
bt-*/
data-1m/
optimize*-result.txt
backtest-1m-result.txt
codex-tt100-result*.txt
validation-latest.json
candles-1m-smoketest.json

# 대용량 데이터 캐시 (재수집 가능)
analyst-hist.json
krx-flows.json
cv2-dump.json
universe-codes.json
candles-1m.jsonl
dtest-tmp.json
research-smoke.json
research-results/*/raw/
stock-keeper-log.txt
data-1m.tar.gz

# 2026-07-30 종가소스 검증용 대용량 캔들 (재생성 가능 — collect-krx-daily.mjs / build-clean-candles.mjs / perturb-candles.mjs)
candles-daily-krx.jsonl
candles-daily-krx-clean.jsonl
candles-daily-toss-clean.jsonl
candles-pert-*.jsonl

# AI 매수 게이트 판단 원장 (런타임 산출물, VM에서 생성)
ai-trader-decisions.jsonl
.ai-trader-state.json
.claude-spawn.lock
# MC/연구 출력물 (재생성 가능)
gap-rsi-*.json
mc-exposure-out.txt
verify-*.jsonl

# 런타임 산출물 (원자쓰기·싱글턴)
.stock-live.pid
stock-live-state.json.*
stock-live-journal.json.*

# 2026-08-07: dbQuery 디스크 캐시(MC 스로틀 회피) · 랭크 계측 덤프
.dbcache/
dump-rank-*.json

# 2026-08-08: 캔들 사본 211MB — candles-daily.jsonl 만 무시돼 있어 사본이 새어나간다.
#   kistail = 20260727~0807 을 KIS 로 덧댄 혼합본(감사 전용, 소스 병기 필수)
#   *.bak-audit = 오염 복원용 원본 백업
candles-daily-kistail.jsonl
*.bak-audit

# 2026-08-22: 연구 산출물·상태파일. 스크립트는 추적하되 그 출력물은 추적하지 않는다.
#   pert-c*.jsonl 은 개당 47MB × 15 = 705MB — candles-pert-* 와 같은 교란본 계열이다.
pert-c*.jsonl
live-scalp-ledger.jsonl
bt-universe-codes.txt
eq-*.json
etf-daily-*.json
flow-intraday/
.entry-monitor-state.json
.fc-tg-state.json

# 2026-08-21 autoresearch 실행 산출물 — 브랜치·세션마다 달라 추적하지 않는다.
#   base.json/probe.json 은 실험 브랜치 국지 상태, log.tsv 는 세션 기록(원본 program.md 도 미추적)
autoresearch-base.json
autoresearch-probe.json
autoresearch-log.tsv
._ar_gate_*.json
